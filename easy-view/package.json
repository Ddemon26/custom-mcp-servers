{
  "name": "easy-view-mcp-server",
  "version": "1.0.0",
  "description": "A read-only MCP server for exploring the current working directory",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts",
    "clean": "rm -rf dist"
  },
  "keywords": ["mcp", "file-explorer", "read-only", "codebase"],
  "author": "Local",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.4.0",
    "fast-glob": "^3.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}